//不允许创建临时变量，交换两个数的内容（附加题）

//预想使用数组进行输入输出

#include <stdio.h>

int main()

{

    printf("交换两个数的内容:");

    int a[2]={0};

    scanf("%d%d",&a[0],&a[1]);

    //scanf("中间不能出现\n，否则会出现无法运行的问题")

    //scanf会自动在%d中间加上忽略空白符号

    //scanf如果中间加上空格，就必须输入空格才能输入第二个数字，如果用回车就不行来→了

    printf("%d %d\n",a[1],a[0]);

    return 0;

}
